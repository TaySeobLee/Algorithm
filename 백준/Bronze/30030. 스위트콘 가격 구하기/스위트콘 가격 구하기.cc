#include <iostream>
using namespace std;

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);
    
    int B = 0;
    cin >> B;
    
    int result = 0;
    result = B *10 / 11;
    cout << result << "\n";
}